{
  "name": "kids-ai-video-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Kids AI short video factory with a Claw Director agent orchestrator.",
  "workspaces": [
    "apps/api",
    "apps/web-admin"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web-admin\"",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web-admin",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "smoke": "npm run smoke --workspace apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
